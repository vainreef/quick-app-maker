interface:
  display_name: "Vainreef Fast Publish V2"
  short_description: "创建 Windows 应用并执行 Microsoft Store 发布流程"
  default_prompt: "Use $vainreef-fast-publish to build this Windows app and continue the existing Microsoft Store workflow."
policy:
  allow_implicit_invocation: true
